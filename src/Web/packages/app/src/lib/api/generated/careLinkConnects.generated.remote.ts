// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { CareLinkConnectStartRequestSchema, CareLinkConnectCompleteRequestSchema } from '$lib/api/generated/schemas';
import { type CareLinkConnectStartRequest, type CareLinkConnectCompleteRequest } from '$api';

/** Begins the connect flow: builds the Auth0 authorize URL and stashes the PKCE verifier server-side.
The client opens AuthorizeUrl in a new tab. */
export const start = command(CareLinkConnectStartRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.careLinkConnect.start(request as CareLinkConnectStartRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in careLinkConnect.start:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to start');
  }
});

/** Completes the connect flow: exchanges the pasted code for a refresh token and stores it as the
connector secret. Returns the discovered username/country for auto-filling the config form. */
export const complete = command(CareLinkConnectCompleteRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.careLinkConnect.complete(request as CareLinkConnectCompleteRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in careLinkConnect.complete:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to complete');
  }
});
